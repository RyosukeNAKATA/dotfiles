-- ==============================================================================
-- nvim-navic 設定
-- 現在のコード位置をパンくずリスト形式で表示
-- （例: Class > method > variable）
-- ==============================================================================

require('nvim-navic').setup {
    lsp = {
        -- LSPサーバーに自動でアタッチ
        -- documentSymbolをサポートするLSPサーバーと連携
        auto_attach = true,
    },
    -- ハイライトを有効化
    -- パンくずリストの各要素に適切なハイライトを適用
    highlight = true,
}
