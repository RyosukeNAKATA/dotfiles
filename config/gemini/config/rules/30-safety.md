# Safety & Protection Rules (30-safety.md)

## 1. 破壊的変更の防止
- 大規模なファイル削除、リポジトリ初期化、不可逆的なコマンド（`rm -rf /` や強制プッシュ `git push --force` 等）をユーザーの確認なしに実行しないこと。
- 一時ファイルが必要な場合は専用の安全なスクリプトディレクトリやワークスペース内を使用すること。

## 2. 機密情報の保護
- APIキー、トークン、秘密鍵、環境変数などの機密情報をコードやログに出力・コミットしないこと。
